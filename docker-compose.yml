version: '3'

services:
  postgres:
    restart: always
    image: postgres:latest
    environment:
      POSTGRES_USER: localuser
      POSTGRES_PASSWORD: localpass
      POSTGRES_DB: baby-victoria
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - '5101:5432'

volumes:
  pgdata:
